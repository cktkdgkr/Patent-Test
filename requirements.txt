anthropic>=0.103
pydantic>=2.0
# truststore makes outbound HTTPS use the OS certificate store so it picks up
# corporate root CAs (Zscaler/Bluecoat/Umbrella style SSL inspection); without
# it, fetches from Google Patents / WIPO / EPO will fail on corporate networks.
truststore>=0.10; python_version>="3.10"
